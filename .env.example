# =============================================================================
# NotebookLM Kit - Environment Variables Configuration
# =============================================================================
# 
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# The .env file is automatically ignored by git (see .gitignore)
# Never commit your .env file with real credentials!
#
# =============================================================================

# =============================================================================
# AUTHENTICATION (Required - Choose ONE method)
# =============================================================================

# Option 1: Auto-login with email/password (Recommended for development)
# - Requires: Account must NOT have 2FA enabled (or use app-specific passwords)
# - First run: Browser opens, authenticates, saves credentials to credentials.json
# - Subsequent runs: Uses saved credentials automatically (no browser prompt)
GOOGLE_EMAIL="your-email@gmail.com"
GOOGLE_PASSWORD="your-password"

# Option 2: Manual credentials (Recommended for production)
# - Get AUTH_TOKEN: Open https://notebooklm.google.com → DevTools (F12) → Console → Run: window.WIZ_global_data.SNlM0e
# - Get COOKIES: DevTools → Network tab → Any request → Headers → Copy Cookie value
# - More secure: No password stored, credentials can be rotated easily
NOTEBOOKLM_AUTH_TOKEN="ACi2F2NZSD7yrNvFMrCkP3vZJY1R:1766720233448"
NOTEBOOKLM_COOKIES="_ga=GA1.1.1949425436.1764104083; SID=g.a0005AiwX...; ..."

# =============================================================================
# DEBUG & LOGGING
# =============================================================================

# Enable debug mode for detailed logging
# - Shows: RPC calls, authentication flow, auto-refresh, streaming, errors
# - Also enabled automatically when NODE_ENV=development
# - Values: true | false (default: false)
NOTEBOOKLM_DEBUG=false

# Alternative debug flags (also work)
# DEBUG=true
# NODE_ENV=development

# =============================================================================
# RETRY CONFIGURATION (Optional)
# =============================================================================

# Maximum retry attempts for failed requests
# - Default: 1
# - Recommended: 1-3 (most requests succeed on first try)
NOTEBOOKLM_MAX_RETRIES=1

# Initial delay between retries (milliseconds)
# - Default: 1000ms
# - Recommended: 1000-2000ms
NOTEBOOKLM_RETRY_DELAY=1000

# Maximum delay between retries (milliseconds)
# - Default: 5000ms
# - Recommended: 5000-10000ms
NOTEBOOKLM_RETRY_MAX_DELAY=5000

# =============================================================================
# CREDENTIAL MANAGEMENT (Optional)
# =============================================================================

# Force re-authentication (ignore saved credentials.json)
# - Use when: Credentials expired, need fresh tokens, troubleshooting auth issues
# - Values: true | false (default: false)
# - Note: Deletes and regenerates credentials.json
FORCE_REAUTH=false

# =============================================================================
# ADVANCED CONFIGURATION (Optional)
# =============================================================================

# Override Google Signaler API key (for credential refresh)
# - Default: Public API key (safe to use as-is)
# - Only override if you have a custom API key
# NOTEBOOKLM_SIGNALER_API_KEY="your-custom-api-key"

# =============================================================================
# EXAMPLE-SPECIFIC VARIABLES (For running examples)
# =============================================================================

# Notebook ID for examples
# - Get from: sdk.notebooks.list() or browser URL
# - Format: UUID (e.g., "9c40da15-f909-4042-bb9e-47fa370b5e3b")
NOTEBOOK_ID="your-notebook-id"

# Multiple notebook IDs (comma-separated, for delete operations)
NOTEBOOK_IDS="notebook-id-1,notebook-id-2,notebook-id-3"

# Artifact ID for examples
# - Get from: sdk.artifacts.list(notebookId)
ARTIFACT_ID="your-artifact-id"

# Source ID(s) for examples
# - Get from: sdk.sources.list(notebookId)
# - Multiple IDs: comma-separated
SOURCE_ID="your-source-id"
SOURCE_IDS="source-id-1,source-id-2"

# Note ID(s) for examples
# - Get from: sdk.notes.list(notebookId)
NOTE_ID="your-note-id"

# =============================================================================
# SLIDE DOWNLOAD (For slide-download-test.ts)
# =============================================================================

# Download format for slides
# - Values: pdf | png
# - Default: Interactive prompt (or 'pdf' if DOWNLOAD_AS is set)
DOWNLOAD_AS="pdf"

# Output directory for downloaded slides
# - Default: ./downloads
# - Creates directory if it doesn't exist
OUTPUT_DIR="./downloads"

# =============================================================================
# VIDEO DOWNLOAD (For artifact-video.ts)
# =============================================================================

# Output directory for downloaded videos
# - Default: ./downloads
# - Note: GOOGLE_EMAIL and GOOGLE_PASSWORD required for video downloads
# OUTPUT_DIR="./downloads"

# =============================================================================
# ARTIFACT DOWNLOAD (For artifact-download.ts)
# =============================================================================

# Output directory for downloaded artifacts (quiz, flashcards, etc.)
# - Default: ./downloads
# OUTPUT_DIR="./downloads"

# =============================================================================
# FILE SOURCE (For source-add-file.ts)
# =============================================================================

# Path to file to upload as source
# - Supports: PDF, images, text files
# - Example: "./documents/research-paper.pdf"
FILE_PATH="./path/to/your/file.pdf"

# =============================================================================
# GOOGLE DRIVE SOURCE (For source-add-drive.ts)
# =============================================================================

# Google Drive file ID
# - Get from: Drive file URL (the ID after /d/)
# - Example: URL is https://drive.google.com/file/d/1a2b3c4d5e6f/view
#            File ID is: 1a2b3c4d5e6f
GOOGLE_DRIVE_FILE_ID="your-drive-file-id"

# =============================================================================
# ARTIFACT CREATION (For artifact-create.ts)
# =============================================================================

# Set default notebook language
# - Values: true | false (default: false)
# - When true: Sets notebook's default language before creating artifacts
SET_DEFAULT_LANGUAGE=false

# Default language code (ISO 639-1)
# - Examples: en, hi, es, fr, de, ja, zh
# - Used when SET_DEFAULT_LANGUAGE=true
# - See: NotebookLMLanguage enum for all supported languages
DEFAULT_LANGUAGE="en"

# =============================================================================
# SHARING (For artifact-share.ts, notebook-share.ts)
# =============================================================================

# User emails to share with (comma-separated)
# - Example: "user1@example.com,user2@example.com"
USER_EMAILS="user1@example.com,user2@example.com"

# Access type for sharing
# - Values: 1 (anyone with link) | 2 (restricted to specified users)
# - Default: 1 if no USER_EMAILS, 2 if USER_EMAILS provided
ACCESS_TYPE=1

# =============================================================================
# ARTIFACT RENAME (For artifact-rename.ts)
# =============================================================================

# New title for artifact
NEW_TITLE="My Renamed Artifact"

# =============================================================================
# UTILITY VARIABLES (For development/debugging)
# =============================================================================

# Extract cookies manually (for utils.ts)
# - Values: true | false
# - When true: Opens browser, waits for manual cookie extraction
EXTRACT_COOKIES=false

# Wait time for cookie extraction (seconds)
# - Default: 60
# - Used when EXTRACT_COOKIES=true
EXTRACT_WAIT_SECONDS=60

# Keep browser open after authentication
# - Values: true | false
# - Useful for debugging authentication flow
KEEP_BROWSER_OPEN=false

# =============================================================================
# NOTES
# =============================================================================
#
# Priority Order (for authentication - checked in this sequence):
#   1. Config options (authToken/cookies in code) - Highest priority, explicit override
#   2. Environment variables (NOTEBOOKLM_AUTH_TOKEN/NOTEBOOKLM_COOKIES) - Explicit config
#   3. Saved credentials (credentials.json in project root) - Automatic reuse from previous session
#   4. Auto-login (GOOGLE_EMAIL/GOOGLE_PASSWORD) - Fallback, requires browser interaction
#
# Why this order? More explicit methods override less explicit ones.
# Environment variables allow you to override saved credentials without deleting credentials.json.
# Best Practices:
#   - Development: Use GOOGLE_EMAIL/GOOGLE_PASSWORD (auto-login)
#   - Production: Use NOTEBOOKLM_AUTH_TOKEN/NOTEBOOKLM_COOKIES (manual)
#   - Never commit .env file to git
#   - Rotate credentials regularly
#   - Use FORCE_REAUTH=true if credentials seem stale
#
# =============================================================================
